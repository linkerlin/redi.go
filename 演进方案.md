# redi.go 演进方案 — Redisson 全面对齐兼容性

> **当前真相（2026-08）**。对照系：`C:/GitHub/redisson` 的 `RedissonClient` 工厂面；
> **Wire / Java 互操作钉 Redisson 4.6.1 + JsonJacksonCodec**（与 `interop/java-probe`、COMPATIBILITY.md 一致）。
> 工作树若新于 4.6.1 的工厂标为 **P-post-4.6.1**，不阻塞 4.6.1 对齐。
> **演进收官**：阶段 0–K 已完成；OSS 可做项已闭环，后续仅维护/按需方法级补齐（见 §2）。
>
> 协作铁律见 [AGENTS.md](AGENTS.md)；状态矩阵见 [COMPATIBILITY.md](COMPATIBILITY.md)「RedissonClient 工厂对齐总表」。

## 1. 目标与原则

1. 对 `RedissonClient` 每一个 `get*`/`create*` 有且仅有一态：`WIRE_OK` / `NATIVE_OK` / `PARTIAL` / `GO_ONLY` / `REFUSE` / `PRO`。
2. 先查 Java 源码（key / channel / Lua / 值编码）；不轻信 redi.py；禁止假实现。
3. companion 一律 `prefixName`/`suffixName` hash-tag；codec 保持 default typing 包装。
4. 方法级不追 100% 镜像，按阶段频率驱动；结构级不留「静默错误互操作」。

```mermaid
flowchart LR
  inventory[RedissonClient工厂清单]
  classify[分类WIRE_NATIVE_PARTIAL_GO_ONLY_REFUSE_PRO]
  phases[阶段0到F]
  gates[验收wire加Java探针加文档]
  inventory --> classify --> phases --> gates
```

## 2. 执行状态

| 阶段 | 内容 | 状态 |
|------|------|------|
| 0 | 方案与矩阵落地（本文 + COMPATIBILITY 总表 + AGENTS 交叉引用） | ✅ |
| A | 互操作硬化：探针盲区 + RateLimiter keepAlive + MultimapCache 后台清扫 + RWLock 文档 | ✅（见阶段 G 续波） |
| B | RJsonBucket / RVectorSet / RSearch（命令缺失 skip） | ✅ |
| C | RClientSideCaching Cluster/Sentinel 能力边界文档化 | ✅ |
| D | LCM：`DisableNearCache` 选项（二进制协议未移植） | ✅ |
| E | Batch 既有门面、Config/RedisNodes、TransferQueue 别名、RMaps | ✅ |
| F | P-post-4.6.1：RCircularBuffer（≠ RRingBuffer） | ✅ |
| G | 互操作续波：NonReentrantFair + MapCacheNative Java 双向；`GetMultiLock` 别名 | ✅ |
| H | MultimapCacheNative（Set/List）+ RingBuffer Java 双向 | ✅ |
| I | SetCache + MultimapCache（Set/List，非 Native）Java 双向 | ✅ |
| J | IdGenerator / Function / ShardedTopic Java 双向 | ✅ |
| K | 收官：Topic / Script / Buckets / Keys / Set / Queue / Multimap / DoubleAdder Java 双向；e2e 补洞；文档终章 | ✅ |
| 维护 | Deque / BlockingDeque / PatternTopic / BlockingQueue / Batch Java 双向；Maps：Go Set → Java RMap 读；MultiLock / RedLock 交叉持锁 | ✅ |

历史 P0–P24 gap-fill（结构补齐至 ~66 `Get*`）已完成，详见文末附录索引；**勿按附录 §0「零互操作 / CRITICAL」再开工单**。

> **演进收官（阶段 K）**：在不做清单（§3.2 诚实偏离 / §3.4 REFUSE / §3.5 PRO）边界内，OSS 可做结构与高价值 Java 互操作探针已闭环。后续仅维护回归、按需方法级补齐，以及模块/版本门槛命令（ARRAY / RedisJSON / VSET / RediSearch / BF.* 等）随运行时能力增强；**不要为凑阶段硬造假 API**。发布切面见 [CHANGELOG.md](CHANGELOG.md) **v0.2.13**。

## 3. 工厂面总账（策略）

### 3.1 已对齐（hardening，不重开结构）

锁族、集合、Map/MapCache(+Native)、Multimap(+Cache/Native)、队列/双端/有界、Delayed、Stream、Topic/Sharded/Reliable、Atomic/Adder、Bucket/BinaryStream、Bloom(+Native)/概率、Geo/HLL/BitSet、RateLimiter、Semaphore/PES/Latch、IdGenerator、TimeSeries、RingBuffer、Function、Keys/Buckets/Script/Batch、Array、Fenced/Multi/RedLock 等。

验收：wire 契约与现有 `TestJavaInterop_*` 不回退。

### 3.2 诚实偏离（禁止悄悄改成 Java）

| 结构 | Java | Go | 策略 |
|------|------|-----|------|
| Priority* | LIST+Comparator | ZSET+score | `GO_ONLY`；不可与 `getPriority*()` 互通 |
| TransferQueue | RemoteService | 队列 LPOP→RPUSH | `GO_ONLY`；提供 `GetQueueTransfer` 别名 |
| LocalCachedMap 失效 | 二进制 invalidation | JSON `{name}:inval` | `PARTIAL`；选项见阶段 D |
| ClientSideCaching | 读代理+EvictionPolicy | go-redis TRACKING | `PARTIAL`；见阶段 C |

### 3.3 OSS 可做缺口（已收口）

| 项 | 策略 | 状态 |
|----|------|------|
| RJsonBucket / RJsonBuckets | RedisJSON `JSON.*`；命令缺失 skip | ✅ 阶段 B |
| RVectorSet | Redis 8+ VSET；命令缺失 skip | ✅ 阶段 B |
| RSearch | RediSearch `FT.*` 核心子集；模块缺失 skip | ✅ 阶段 B |
| RMaps | 批量 map 导入门面（4.6.1 有则对齐） | ✅ 阶段 E/F |
| RCircularBuffer | **P-post-4.6.1**；ARRAY 环，≠ RRingBuffer | ✅ 阶段 F |
| getId / getConfig / getRedisNodes | 客户端身份、只读配置快照、节点探测薄封装 | ✅ 阶段 E |
| 方法级 | RateLimiter keepAlive、MultimapCache 可选后台清扫、RBatch 扩面、RObject 工具面 | ✅ 阶段 A/E |
| 互操作硬化收官 | Topic / Script / Buckets / Keys / Set / Queue / Multimap / DoubleAdder | ✅ 阶段 K |

### 3.4 REFUSE_AS_FAKE

RSortedSet（Comparator）、RTransaction（勿用 MULTI/EXEC 冒充）、RRemoteService / 真 TransferQueue、RExecutorService、LiveObject、reactive。

### 3.5 PRO-only（开源 UOE）

`RReliableQueue`、`RLocalCachedMapCache`、`RReliablePubSubTopic`、`RBitVectorStore` — 不做 stub。

## 4. 分阶段细则

### 阶段 0 — 文档

- 本文为当前对齐方案。
- COMPATIBILITY「RedissonClient 工厂对齐总表」一厂一态。
- AGENTS「已知不做」交叉引用本文 §3.4–3.5。

### 阶段 A — 互操作硬化

- Java 探针：Spin / NonReentrant / NonReentrantFair、AtomicDouble、BoundedBlockingQueue、MapCacheNative（HPEXPIRE 缺失 skip）——见阶段 G。
- RateLimiter：可选 keepAlive 刷新与过期 config 清理。
- MultimapCache：`StartAutoEviction` 可选后台清扫（默认仍惰性）。
- RWLock：混部 timeout companion 与 Java 不完全同形 — 文档诚实；互斥语义保持 WIRE_OK。

### 阶段 B — Redis 原生新结构

`GetJsonBucket` / `GetJsonBuckets` / `GetVectorSet` / `GetSearch`；测试 skip 友好；COMPATIBILITY 新行。

### 阶段 C — ClientSideCaching

- standalone DB0 RESP3 路径保持。
- Cluster/Sentinel：尝试接线；失败则 PARTIAL 写明原因，禁止宣称兼容 Java EvictionPolicy。

### 阶段 D — LocalCachedMap

- 调研结论：Java invalidation 强依赖 Java 序列化形态 → **不移植二进制协议**。
- API：`LocalCachedMapOptions`（`InteropGoOnly` 默认；可关近端缓存用于混部）。

### 阶段 E — 工程收口

- RBatch：List/Set/Deque/AtomicDouble。
- `Client.ID()`、`Client.Config()` 只读快照、`GetRedisNodes()`。
- `GetQueueTransfer` 别名；覆盖率目标根包 ≥80%（环境 skip 不硬卡）。

### 阶段 F — post-4.6.1

- `RCircularBuffer`（Redis 8.8+ ARRAY）。
- `RMaps` 批量导入。

### 阶段 G — 互操作续波

- Java 探针：`getNonReentrantFairLock`、`getMapCacheNative`（HPEXPIRE 缺失 skip）。
- `Client.GetMultiLock` 对齐 Redisson 命名（同 `NewMultiLock`）。
- 验收：新增 `TestJavaInterop_RNonReentrantFairLock` / `TestJavaInterop_RMapCacheNative`。

### 阶段 H — Native Multimap / RingBuffer 互操作

- Java 探针：`getSetMultimapCacheNative` / `getListMultimapCacheNative`（expireKey + put/getAll）与 `getRingBuffer`。
- 验收：`TestJavaInterop_RSetMultimapCacheNative` / `RListMultimapCacheNative` / `RRingBuffer`。

### 阶段 I — SetCache / MultimapCache 互操作

- Java 探针：`getSetCache`（TTL add/contains/size）、`getSetMultimapCache` / `getListMultimapCache`。
- 验收：`TestJavaInterop_RSetCache` / `RSetMultimapCache` / `RListMultimapCache`。

### 阶段 J — IdGenerator / Function / ShardedTopic

- Java 探针：`getIdGenerator`（alloc=1 Go/Java 交错发号无重复）、`getFunction`（LOAD/FCALL，缺失 skip）、`getShardedTopic`（SPUBLISH 双向）。
- 验收：`TestJavaInterop_RIdGenerator` / `RFunction` / `RShardedTopic`。

### 阶段 K — 演进收官

- Java 探针：`getTopic`、`getScript`（EVAL/SCRIPT LOAD）、`getBuckets`、`getKeys` 子集、`getSet` / `getQueue`、`getSetMultimap` / `getListMultimap`、`getDoubleAdder`。
- e2e：`TestE2E_PhaseK_BucketsScriptMultimap`。
- 文档：本文终章、COMPATIBILITY / 五语 README 互操作计数校准为 **48** 个 `TestJavaInterop_*`。
- **不做**：不为凑阶段新增 stub；LCM 二进制失效 / Transaction / Comparator SortedSet / PRO-only 仍按 §3.2–3.5。

## 5. 每结构强制流程

1. 打开 `C:/GitHub/redisson/.../RedissonXxx.java`（及 `redisson-4.6.1` tag）。
2. 记录 key / channel / Lua / 值编码。
3. 嵌入 `rObject` + codec；companion hash-tag。
4. wire 契约（若需）+ 单元/e2e + 可选 Java 探针。
5. 同步 README / README_EN / COMPATIBILITY / CHANGELOG / 本文执行状态。

## 6. 成功定义

- 工厂总表一厂一态、无歧义。
- 4.6.1 主路径无回归；新结构有版本/模块门槛与 skip。
- 本文文首为当前真相；附录历史底稿不再误导。
- **阶段 K 起**：对齐演进在不做清单内已完成；后续仅维护 / 按需方法级补齐（见 §2 收官说明）。

---

## 附录 A：历史执行索引（P0–P24，已完成）

早期从 6 结构 → ~66 `Get*`、wire 24 组、Java interop 24+、codec/DelayedQueue/RateLimiter 正确性修复等，详见 git 历史与 CHANGELOG Unreleased / v0.2.x。

## 附录 B：2026-08 初审底稿（过时）

> 以下保留作考古。**当时**约 6 结构、私有 key 前缀、watchdog CRITICAL、gotrycatch 样板——均已在 P0–P5 及后续轮次消除。
> **禁止**按下列 CRITICAL /「零互操作」数字再开修复单。

### B.0 审计结论速览（历史）

| 维度 | 当时现状 | 当时问题 |
|------|----------|----------|
| 数据结构 | 6 个 + dashboard | 相对 Redisson 缺口大 |
| 互操作 | 零 | 私有前缀 / 编码未对齐 |
| 正确性 | watchdog 固定 10s | TTL≤10s 锁可被静默双写 |
| 代码质量 | gotrycatch ~40% | 已删除 |

历史分阶段 P0–P5 任务表、问题完整索引已完成并归档于 git：`演进方案.md` 在全面对齐改版前的版本。需要细节时用 `git log -p -- 演进方案.md` 回溯。
