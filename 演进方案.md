# redi.go 演进方案

> 基于 2026-08 对 `redi.go` 全量代码的审计（约 1,100 行源码 / 8 个模块 + 6 个测试文件）。
> 参考系：`C:/GitHub/redisson`（Java 原版 4.6.x）与 `C:/GitHub/redi.py`（Python 版，70 模块 / 694 tests，
> 其 wire-format 已与真实 Redisson 双向实测验证 —— 本方案直接复用其已验证结论，避免二次踩坑）。
>
> 当前状态：核心路径可用、测试可跑，但存在**正确性 CRITICAL bug**（watchdog 续期间隔）、
> **零互操作性**（key 命名/值编码均与 Redisson 不兼容）、以及大规模样板代码（gotrycatch）。

## ✅ 执行状态（2026-08-15 更新）

| 阶段 | 内容 | 状态 |
|------|------|------|
| P0 | 静默正确性 bug（C1/C2/C3 watchdog、CAS 缺 key、Take 截断、Close 停后台） | ✅ 完成 |
| P1 | 去 gotrycatch/GoExecutors、dashboard 死代码 | ✅ 完成 |
| P2 | wire 对齐（裸 key、Codec 层、RLock Redisson Lua + pub/sub 唤醒、hash-tag 规范） | ✅ 完成 |
| P3 | rObject 基类、UniversalClient（single/cluster/sentinel）、工厂别名 | ✅ 完成 |
| P4-A | RSemaphore / RCountDownLatch / RReadWriteLock | ✅ 完成 |
| P4-B | RBucket / RDeque / RBlockingQueue / RScoredSortedSet / RAtomicDouble | ✅ 完成 |
| P4-C | RRateLimiter / RDelayedQueue / RTopic / RPatternTopic | ✅ 完成 |
| P4-D | RBloomFilter / RIdGenerator / RMapCache | ✅ 完成 |
| P5 | wire 契约测试 + CI（redis:8 + race）+ README/CHANGELOG/COMPATIBILITY | ✅ 完成 |

**最终状态**：结构数 6 → 21，`go vet` / `go test -race` 全绿（含 `wire_compat_test.go` 的 8 个 wire 契约断言）。
偏离项：P3-4 泛型化读取改为 `Codec` + `RBucket.GetInto`（更 Go 惯用）；P5-2 miniredis 未引入（CI 用 redis:8 service 容器覆盖）。
**追加完成（同日）**：Go ↔ redi.py 双向互操作回归（`interop/redipy_test.go`，8 组用例：锁互斥/释放、Map 三类型值双向、AtomicLong、Bloom 哈希一致、RateLimiter 共享窗口、Semaphore 跨进程、Latch 跨语言唤醒、DelayedQueue ZSET——经 redi.py 传递性对齐真实 Redisson）；`benchmark_test.go` 基准（P5-4）。
**追加完成（第二轮）**：RMapCache entry 事件通道（created/updated/removed/expired，Redisson 实测的 8 字节 LE 长度前缀二进制格式，`AddListener`/`RemoveListener` + wire 契约断言）；RReadWriteLock watchdog（`ttl<=0` 续期 lease/3，与 RLock 同款生命周期护栏）。
**追加完成（第三轮）**：RSetCache（单 ZSET score=过期时刻 + maxIdle 刷新）、RBlockingDeque（双端阻塞）、RMultimap Set/List 双实现（内部 ID = HighwayHash-128 大端 base64，与 redi.py 字节级互验 + 双向读写互操作测试）。结构数 21 → 26。
**追加完成（第四轮）**：**Go ↔ Java Redisson 4.6.1 直接双向互操作回归**（`interop/java-probe/` 单 JVM REPL 探针 + `interop_java_test.go`，7 组用例：锁双向互斥、Map 三类型值双向、AtomicLong、Bloom 哈希/公式一致、RateLimiter 共享窗口、Semaphore、Latch 跨语言唤醒）；发现并修复 **codec 缺口**：JsonJacksonCodec default typing 读裸 JSON 对象报错 → 编码端补 `@class`（LinkedHashMap）与 `["java.util.ArrayList",...]` 包装（wire 契约锁定；redi.py 写裸对象存在此隐藏缺陷）。另增 RLexSortedSet（裸成员字典序）与 RTransferQueue（原子迁移）。结构数 26 → 28。
**追加完成（第五轮）**：工具类三件套 —— RKeys（SCAN 迭代/模式删除/Copy/Type/FlushDB）、RBuckets（MSET/MGET/MSETNX + 批量 TTL）、RScript（EVAL/EVALSHA + ReturnType 转换）。结构数 28 → 31。
**追加完成（第六轮）**：**RBatch 管道批处理** —— `cmds redis.Cmdable` 覆写点（Pipeliner/Client 同接口）+ 9 个批量 facade + Execute/Discard；基准 10 Put **7.1x** 加速（2.16ms→0.30ms，对齐 redi.py 的 ~11x 量级结论）；订阅操作固定路由真实连接（不可批处理）。
**追加完成（第七轮，收官）**：dashboard 三页签（INFO / Locks 持锁者+重入+TTL / Limiters 配置+剩余许可，`Run(rc, patterns...)` 可指定锁扫描范围）+ dashboard 单元测试。
**追加完成（第八轮，v0.2.1）**：Java 直接互操作 7 → **14 组**（新增 RWLock/List/ScoredSortedSet/LexSortedSet/Bucket/MapCache/DelayedQueue）；**RDelayedQueue 按 Java 实测重写为 Redisson 4.6.1 真实布局**（timeout ZSET + 内部 LIST 双结构、Bc0Lc0 packed member 小端 8 字节长度前缀、原版迁移 Lua）——发现 redi.py 的单 ZSET 实现是 4.x 前旧格式、与 Java 不互通（其自身宣称有误）。
**追加完成（第九轮，v0.2.2）**：RHyperLogLog（PFADD/PFCOUNT/MERGE）、RGeo（GEOSEARCH 而非 Redis 8 已删的 GEORADIUS + Lua 原子 NX TryAdd）、RBitSet（**源码验证 Java 原生位序无反转**——纠正 redi.py 的位反转 length 错误；BITOP 全家 + 字节往返）。Java 互操作 14 → **17 组**。结构数 34。
**追加完成（第十轮，v0.2.3）**：RStream（消费组全生命周期：CreateGroup/ReadGroup/PendingRange/Ack/Claim/AutoClaim + MAXLEN/MINID 裁剪；field 与值双 codec 编码对齐 Redisson）。修复 go-redis `Block:0`=永久阻塞陷阱。Java 互操作 17 → **18 组**。结构数 35。
**追加完成（第十一轮，v0.2.4）**：RPermitExpirableSemaphore（许可级租约 + 惰性过期回收）、RReliableTopic（**源码验证 Java 为每订阅者独立消费组**——纠正 redi.py 单组负载均衡的语义错误；field `m` + 回调后 XACK + timeout 活性 zset）。Java 互操作 18 → **19 组**。结构数 37。
**追加完成（第十二轮，v0.2.5）**：RLocalCachedMap（近端缓存：本地命中零往返 + 写穿 + 跨实例失效广播 + 容量上限；数据层 RMap wire ✅ Java 双向实测，失效广播为 Go 内部协议——Java 的 keyHash 二进制协议未复刻，诚实标注）。Java 互操作 19 → **20 组**。结构数 38。
**追加完成（第十三轮，v0.2.6）**：Codec Encode 标量快路径（**~25x**：字符串/整数 63-103ns vs 原 2.4µs 三段管线）；深层嵌套复合值经 Redisson 4.6.1 实测（第 21 组用例）；RPriorityQueue（ZSET 优先级队列）。结构数 39。
**追加完成（第十四轮，v0.2.7）**：**RRateLimiter 双重正确性修复**（示例测试触发的真 bug：redi.py 字节手算读写端序不一致→过期归还 16777216/个；固定 id→ZADD 塌缩重复获取→许可泄漏；AvailablePermits 裸删不回池）—— 均以 Redisson 原版 struct.pack/unpack + 每次随机 id 修复并加回归测试。golangci-lint 清零（13 项）并纳入 CI（checkout@v5）；godoc 示例测试 5 个。
**追加完成（第十五轮，v0.2.8）**：RLongAdder/RDoubleAdder（**源码复刻 BaseAdder 跨语言协议**：topic 广播 + 信号量栅栏 + GETDEL 汇总，Java 实例直接参与 Go 发起的 Sum）；修复订阅就绪竞态与 PubSub ctx 死锁两处生命周期 bug；AGENTS.md 沉淀全部踩坑铁律。Java 互操作 21 → **22 组**。结构数 41。
**追加完成（第十六轮，v0.2.9）**：wire 契约测试 7 → **16 组**（补 RWLock/Semaphore/Latch/PES/Stream/ReliableTopic/LongAdder/BitSet/LocalCachedMap）—— CI 无 JVM 时契约测试是唯一 wire 防线；修复 pub/sub 断言的订阅注册竞态。
**追加完成（第十七轮，v0.2.10）**：RFencedLock（栅栏令牌锁：每次获取 INCR `redisson_lock_token:{name}`，重入也递增，acquire Lua 同 Java 原版）、RMultiLock（全有或全无 + 失败回滚）。RTransaction 经查为快照回滚语义，记入不做清单。wire 契约 17 组。结构数 43。
**追加完成（第十八轮，v0.2.11）**：RTimeSeries（源码复刻：序列号 member + 双 Lua 变体 TTL + label mark 字节 + 惰性 size；Java 互操作 22 → **23 组**双向实测）。结构数 44。
**追加完成（第十九轮，v0.2.12）**：RRingBuffer（定容环形缓冲，Lua 同 Java 原版）、RShardedTopic（Redis 7+ SSUBSCRIBE 分片 pub/sub）、RFunction（FUNCTION/FCALL 门面）—— 三者均 Redis 原生协议跨语言互通。结构数 47。
**v0.2.0 全部范围就绪**：47 结构 + 4 工具 facade + RBatch + dashboard，Go↔Redisson 4.6.1（23 组）/ redi.py 双向互操作验证 + 17 组 wire 契约，CI 全绿。

---

---

## 0. 审计结论速览

### 0.1 现状盘点

| 维度 | 现状 | 问题 |
|------|------|------|
| 数据结构 | 6 个（RLock/RMap/RList/RSet/RAtomicLong/RQueue）+ TUI dashboard | Redisson ~100 接口，redi.py 已覆盖 65+ |
| 互操作 | **零** | key 带私有前缀 `redi:lock:` 等；值无统一编码层；锁 Lua 自创变体 |
| 正确性 | 1 个 CRITICAL | watchdog 固定 10s 续期，TTL < 30s 时**锁必然提前过期**，互斥被静默破坏 |
| 代码质量 | gotrycatch 包装占源码 ~40% | 每个方法 8 行 Try/panic/Catch 样板替代 1 行 `return err` |
| 依赖 | gotrycatch + GoExecutors（作者自研） | GoExecutors 每次 Lock 新建线程池；gotrycatch 用 panic 传 error |
| 测试 | 6 个测试文件，全部依赖本地 Redis | 无 Redis 时静默 skip；无 CI；无 wire-format 契约测试 |
| 部署形态 | 仅单机 `*redis.Client` | 无 Cluster/Sentinel（go-redis 的 UniversalClient 一行可换） |

### 0.2 问题分级

| 级别 | 数量 | 含义 |
|------|------|------|
| 🔴 **CRITICAL** | 3 | 静默破坏互斥语义 / 持锁者丢失续期 / 泄漏 |
| 🟠 **MAJOR** | ~10 | 零互操作、语义偏差、结构性缺失 |
| 🟡 **MINOR** | ~8 | API 表面缺口、死代码、文档漂移 |

### 0.3 最具破坏力的问题

| # | 位置 | 根因 | 后果 |
|---|------|------|------|
| C1 | `rlock.go:18,196` | watchdogInterval 恒为 10s，与 TTL 无关 | **TTL ≤ 10s 的锁在两次续期间过期**，另一个客户端可获锁 → 双写。现有测试用 5s TTL 之所以通过，仅因临界区极短 |
| C2 | `rlock.go:99-105,139-146` | 重入 Lock 会替换 cancelWatchdog（取消上一个） | 重入后仅剩一个 watchdog，且首次 Unlock 即取消它 → 仍持锁（计数 1）但无续期，锁静默过期 |
| C3 | `rlock.go:157-164` | Unlock 在校验所有权**之前**就取消 watchdog | 非持有者误调 Unlock（或 clientID 拼错）会杀掉真正持有者的续期 |

---

## 1. 演进原则

1. **根因优先（ponytail）**：修在共享层（基类/Lua/编码），不在每个 caller 打补丁。
2. **互操作即真理**：单进程测试全绿 ≠ 兼容。凡 wire-format 改动，以 redi.py 已实测的 Redisson 布局为唯一依据，并新增契约测试。
3. **回归不破**：每阶段现有测试不退化，修复必配最小失败用例。
4. **诚实降级**：暂不实现的结构在 COMPATIBILITY.md 标注，不静默给错误实现。
5. **文档同步**：每阶段完成同步 README / CHANGELOG / COMPATIBILITY.md（三者当前已与代码漂移：README 称"复刻 Redisson"，实际零互操作）。

---

## 2. 分阶段路线图

> 优先级 = (正确性风险 × 互操作阻塞度 × 使用频率)。
> 每阶段四段式：**目标 / 任务（file:line 现状 → 修复）/ 验收 / 不做**。

```
P0 (正确性, 1-2d) ──→ P1 (去样板/删依赖, 1d) ──→ P2 (wire 对齐, 3-5d) ──→ P4 (新结构, 按需)
                                                │
                                                └→ P3 (基类+UniversalClient, 1-2d) 可与 P2 并行
P5 (工程化/CI, 0.5d) 穿插进行
```

---

### 阶段 P0 — 正确性 bug 修复（1-2 天，无 API 破坏）

**目标**：消除"测试通过但生产即崩"的静默隐患。改动小、收益极高。

| 任务 | 文件:行 | 现状 → 修复 |
|------|---------|------------|
| P0-1 watchdog 间隔 = TTL/3 | `rlock.go:18,196` | 固定 `watchdogInterval=10s` → `ttl/3`（Redisson 语义：internalLockLeaseTime/3），下限 1s |
| P0-2 重入不叠 watchdog | `rlock.go:99-106` | 单个 `cancelWatchdog` 字段被替换 → 以 clientID 为 key 的 `map[string]*renewState`（cancel + refcount）；重入仅 refcount+1，不新建续期 |
| P0-3 Unlock 先验证后取消 | `rlock.go:157-164` | 先 cancel 再跑脚本 → 先跑脚本，仅当**完全释放**（剩余计数≤0）才 cancel 对应 clientID 的 watchdog |
| P0-4 renew 失败可观测 | `rlock.go:204-211` | `_ = Catch(...)` 全吞 → 区分：`renew 返回 0`（已非持有者）→ 停止该续期；网络错误 → 重试 + `log` 钩子（可注入 Logger，默认 log.Printf） |
| P0-5 续期器降级路径删除 | `rlock.go:184-193` | pool 创建失败则**静默无 watchdog**（锁会过期而调用者不知）→ P1 将整体替换为原生 goroutine，此问题随之消失；过渡期先加 log |
| P0-6 RAtomicLong.Get 缺失语义 | `ratomiclong.go:26-30` | 缺 key 返回 `redis.Nil` 错误 → Redisson 语义：返回 `(0, nil)` |
| P0-7 CAS 缺失 key 语义 | `ratomiclong.go:100-107` | key 不存在时 `cur=false == ARGV[1]` 恒 false → Lua 顶部 `if not cur then cur = "0" end` + 两侧 `tonumber` 比较；`expect=0` 对缺失 key CAS 成功（Redisson 行为） |
| P0-8 Take 亚秒超时截断 | `rqueue.go:73-77` | BLPop 的 timeout 参数为秒精度，`500ms` 截断为 0 = **永久阻塞**（仅靠 ctx 兜底）→ `<1s` 上取整到 1s，文档标注 |
| P0-9 Close 不断 watchdog | `client.go:78-80` | Close 后所有 watchdog 永远空转刷错误 → Client 持 root ctx，Close 时 cancel，全部续期 goroutine 退出 |

**验收**：
- 新增测试：TTL=2s 的锁持续持锁 10s，期间另一 client TryLock 必败（当前代码必挂，即 C1 的失败用例）。
- 新增测试：重入 2 次 → Unlock 1 次后锁仍被续期（C2 用例）；错误 clientID Unlock 不影响持有者续期（C3 用例）。
- 现有测试全绿。

**不做**：不改 key 命名、不动 Lua 脚本算法（留给 P2 统一重写），P0 只保证"自洽正确"。

---

### 阶段 P1 — 去 gotrycatch / 去 GoExecutors（1 天，纯内部重构）

**目标**：删除两大反模式依赖，源码预计减 35-40%，行为完全不变。

| 任务 | 范围 | 修复 |
|------|------|------|
| P1-1 删 gotrycatch 包装 | 全部 `r*.go`（每个方法 8 行 Try/panic/Catch → 1 行直接 `return err`） | 如 `rmap.go:22-32` 的 Put 变 3 行。panic-recover 传 error 非惯用且丢栈 |
| P1-2 删 GoExecutors | `rlock.go:184-214` | 每次 Lock 新建 1 线程池（churn + 泄漏风险）→ 原生 `go func` + `time.Ticker`（每持锁一个 goroutine，成本可忽略，与 Redisson 每条目一个 ScheduledFuture 对齐） |
| P1-3 清理 go.mod | `go.mod` | 删除 `gotrycatch`、`GoExecutors` 两个 require |
| P1-4 dashboard 死代码 | `dashboard.go:66,149,174-175` | `viewport` 被 Update 但从未出现在 View() → 删除（或后续真正用于 raw INFO 展示，二选一，建议删） |

**验收**：`go test ./...` 全绿；`grep -r gotrycatch\|GoExecutors` 零命中；行数对比写入 CHANGELOG。
**不做**：不引入新依赖替代（标准库足够）。

---

### 阶段 P2 — wire-format 对齐 Redisson，实现互操作（3-5 天）

**目标**：与 Java Redisson（JsonJacksonCodec 配置）/ redi.py 互读写。这是本项目的定位兑现。
布局全部取自 redi.py 经双向实测验证的结论（`redi.py/兼容性优化演进方案.md` §0.1），不重复踩坑。

| 任务 | 现状 → 目标 |
|------|------------|
| P2-1 key 去私有前缀 | `redi:lock:x` / `redi:map:x` 等 → 裸 `{name}`（Redisson 默认 NameMapper=identity）。可选 `NameMapper` 配置保留转义能力 |
| P2-2 值编码层 | 无 → `Codec` 接口 + 默认 `JSONCodec`（`encoding/json`，读写容忍并剥离 Java `@class` 字段），对齐 redi.py SerializationService 行为；RMap/RList/RSet/RQueue 统一走 codec |
| P2-3 RLock 重写 | 自创 Lua → Redisson 原版布局：HASH `{name}`、field=`{uuid}:{threadId}`（Go 侧：Client 持 uuid，field 由 caller token 或 lock 实例 token 组成，文档写明与 Java threadId 的对应）、channel=`redisson_lock__channel:{name}`，unlock Lua 含 PUBLISH |
| P2-4 锁等待 pub/sub 唤醒 | 50ms 轮询（`rlock.go:113`）→ 订阅 unlock channel 即刻重试 + 1s 兜底（redi.py 实测唤醒延迟 ~0ms，轮询为 50-100ms） |
| P2-5 watchdog 对齐 | 续期用 Redisson renew Lua（hexists 判定），interval=leaseTime/3 |
| P2-6 RAtomicLong 编码 | Redisson 为十进制字符串（LongCodec）→ 现有 INCR/DECR 天然兼容，仅补契约测试锁定格式 |
| P2-7 Cluster hash-tag 规范 | companion key 一律 `{name}:xxx` 形式（redi.py 阶段 43 实测：CRC16(name) 天然同槽，多 key Lua 无 CROSSSLOT），为 P2 新增结构与 P3 cluster 铺路 |

**验收**：
- 新增 `tests/test_wire_compat.go`：锁的 HASH field 布局、channel 名、值 JSON 格式逐项断言（对标 redi.py `test_wire_compat.py` 的 42 例契约思路，先覆盖已有 6 结构）。
- 可选（有环境时）：Go 写 → Java Redisson 读 / 反向，各 1 例（对齐 redi.py 的 run-interop 模式）。
- README 的"复刻 Redisson"宣称从此有据。

**风险**：key 前缀移除是破坏性变更（老数据不迁移）→ 版本号升 v0.2.0，CHANGELOG 醒目标注 + 提供 `WithLegacyPrefix()` 兼容开关（一行 opt-in）。

---

### 阶段 P3 — 基类统一 + 多部署形态（1-2 天，可与 P2 并行）

**目标**：消除 6 个结构间的复制粘贴，补 RObject/RExpirable 表面，支持 Cluster/Sentinel。

| 任务 | 现状 → 目标 |
|------|------------|
| P3-1 `rObject` 基类 | 6 个结构各自持 `rc/name/key` 且各写 Clear → 嵌入式基类，统一 `Delete/Exists/Touch/Unlink/Expire/ExpireAt/RemainTTL`（对齐 Redisson RObject/RExpirable；redi.py P4-A 同款收敛，其收益已被验证） |
| P3-2 UniversalClient | `client.go:49` 仅 `*redis.Client` → `redis.UniversalClient`；Config 增加 `Mode(single/cluster/sentinel)` + 地址列表，dashboard 签名同步放宽（`dashboard.go:75`） |
| P3-3 工厂别名 | `GetRLock` 等 → 补 Redisson 命名别名 `GetMap/GetList/GetLock/...`（保留现名，双命名并存，README 标注） |
| P3-4 泛型化读取 | Get 返回裸 string → `RMap[K,V]`/`RBucket[T]` 泛型工厂（Go 1.25，零成本；codec 层 P2-2 已铺路） |

**验收**：继承基类的结构比例 100%；`NewClient` 连 cluster 3 节点跑通冒烟（无环境则条件 skip 测试）。

---

### 阶段 P4 — 结构扩面（按需滚动，每结构半天）

**目标**：以 redi.py 已验证布局为准，优先补高频结构。每补一个 = 一次 wire 契约测试。

| 批次 | 结构 | 关键布局（源自 redi.py 实测） |
|------|------|------------------------------|
| P4-A 同步原语 | RSemaphore / RCountDownLatch / RReadWriteLock | RWLock：单 HASH + mode 字段 + `{name}:{cid}:{tid}:rwlock_timeout:1` 续期 key（**勿拆双 key，非原子且零互操作** —— redi.py C2 教训）；Latch：DECR+DEL+PUBLISH，channel 名对齐 |
| P4-B 高频容器 | RBucket / RDeque / RBlockingQueue / RScoredSortedSet | RBucket：TTL 毫秒精度（PSETEX；redi.py P0-9 教训：SETEX 秒截断）；值 JSON + Long/Double 包装兼容 |
| P4-C 协调与消息 | RRateLimiter / RDelayedQueue / RTopic / RPatternTopic | RateLimiter：config HASH `{name}`（rate/interval/type=枚举序数）+ `{name}:value` + ZSET `{name}:permits` 二进制 member（**配置必须持久化到 Redis**，否则跨进程失效 —— redi.py C3 教训）；DelayedQueue：ZSET `redisson_delay_queue:{name}` + 同名 channel；Topic：独立订阅 goroutine（**publish 不得本地 fan-out 双派发** —— redi.py P0-6 教训） |
| P4-D 进阶 | RBloomFilter / RIdGenerator / RMapCache | Bloom：HighwayHash-128 + config HASH `{name}:config`，位公式用 Java 语义（截断非 ceil、半上取整非 banker's）；MapCache：timeout ZSET `redisson__timeout__set:{name}` + idle ZSET，entry 结构 8 字节 double + 8 字节 long |
| P4-E 按需 | RMultimap / RLock 变体（FencedLock 等） | Multimap 内部 ID = `Hash.hash128toBase64(codec(key))`（redi.py 阶段 37 已解密） |

**验收**：每结构 ≥1 个 wire 契约测试 + 常规功能测试；COMPATIBILITY.md 同步登记。

---

### 阶段 P5 — 工程化（0.5 天，穿插）

| 任务 | 说明 |
|------|------|
| P5-1 CI | GitHub Actions：`go vet` + `golangci-lint` + `go test`（services: redis:8 容器），替代当前"无 Redis 静默 skip" |
| P5-2 双层测试 | 引入 `miniredis`（支持 Lua/绝大多数命令）跑离线单测；真 Redis 跑集成层 —— 本地/CI 无 Redis 也能守住回归 |
| P5-3 文档 | 新增 COMPATIBILITY.md（兼容矩阵）；README 重写"当前兼容范围"；CHANGELOG 起版 |
| P5-4 基准 | `go test -bench` 对标 redi.py 基准维度（单操作 ops/s、pipeline 加速比），为 codec 层开销提供数据 |

---

## 3. 横切关注点

- **错误处理策略**：P1 后统一为直接返回；后台 goroutine 的错误走可注入 Logger（默认 `log` 包），禁止 `_ =` 吞错（P0-4 落地）。
- **文档漂移**：README 目前宣称与 Redisson 对齐但零互操作 —— P2 之前先加"当前为独立实现，尚未互操作"的诚实标注。
- **测试基建**：`tests/test_wire_compat.go` 作为 wire 契约单一真相源，此后每个格式改动必加一例（redi.py 模式，已被 694 个测试验证有效）。

## 4. 度量与验收

### 4.1 每阶段 DoD
1. 现有测试全绿（不退化）。
2. 修复项附带最小失败用例。
3. 涉及 wire-format 的改动有契约测试。
4. README/CHANGELOG/COMPATIBILITY.md 同步。

### 4.2 全局 KPI

| 指标 | 当前 | 目标 |
|------|------|------|
| CRITICAL 正确性缺口 | 3 | 0（P0） |
| 源码行数（样板占比） | ~1,100（~40% 样板） | P1 后 -35% |
| gotrycatch/GoExecutors 依赖 | 2 | 0 |
| 与 Redisson 互操作结构数 | 0 | 6（P2）→ 15+（P4） |
| CI（无 Redis 环境可跑） | 无 | 有（P5） |
| 部署形态 | single | single/cluster/sentinel（P3） |

## 5. 不做清单（明确排除）

- ❌ 不实现 Redisson reactive/RxJava 绑定（Go 无对应范式；go-redis 本身有 pipeline/事务可直接暴露）。
- ❌ 不追求 100% 接口覆盖（Redisson ~100 接口 × 4 套范式；聚焦 sync 一套，泛型即"类型化"）。
- ❌ 不重写连接池（go-redis 内置，redi.py 详细设计方案 §2.1 同款决策）。
- ❌ 不做 RExecutorService/RRemoteService 的 Java 互操作（跨语言 RPC 序列化需求低，按需再排期）。
- ❌ RSortedSet（LIST+Comparator 版）暂不实现，勿用 ZSET 冒充（redi.py C7 教训）。

---

## 附：问题完整索引（file:line）

### 🔴 CRITICAL
- `rlock.go:18,196` — watchdog 固定 10s，TTL<30s 锁提前过期（C1）
- `rlock.go:99-106,139-146` — 重入替换 cancelWatchdog，续期丢失（C2）
- `rlock.go:157-164` — Unlock 先取消 watchdog 后验权（C3）

### 🟠 MAJOR
- `rlock.go:38` 等 6 处 — 私有 key 前缀，零互操作
- `rlock.go:113` — 50ms 轮询等待，无 pub/sub 唤醒
- `rlock.go:184-193` — 每次 Lock 新建线程池；失败静默无 watchdog
- `rlock.go:204-211` — renew 错误全吞
- `ratomiclong.go:26-30` — Get 缺 key 报 redis.Nil（Redisson 返 0）
- `ratomiclong.go:100-107` — CAS 缺 key 恒失败
- `rqueue.go:73-77` — Take 亚秒超时 → 永久阻塞
- `client.go:49` — 仅 *redis.Client，无 cluster/sentinel
- `client.go:78-80` — Close 不停 watchdog
- 全部 `r*.go` — 无 RObject/RExpirable 基类，无 codec 层
- `rmap.go:35` 等 — Get 直接暴露 redis.Nil 给调用者（待 P2 codec 层统一语义）

### 🟡 MINOR
- `dashboard.go:63,75` — 收窄为 *redis.Client
- `dashboard.go:66,149,174-175` — viewport 死代码
- 无 CI / 无 lint 配置 / 无 CHANGELOG
- README 互操作宣称与实际不符
- 测试无 Redis 时静默 skip，无离线兜底
